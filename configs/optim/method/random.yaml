# @package optim.method
# Sobol random search: the control. Matched to turbo on evaluations and seed, so
# its first n_init points are turbo's initial design.

name: random

budget:
  # Sobol draws labelled `init`, to line up with turbo's phase boundary.
  n_init: 32
  n_iter: 160
